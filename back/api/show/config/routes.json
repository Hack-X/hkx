{
  "routes": [
    {
      "method": "GET",
      "path": "/shows",
      "handler": "Show.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/count",
      "handler": "Show.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/:_id",
      "handler": "Show.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/shows",
      "handler": "Show.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/shows/:_id",
      "handler": "Show.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/shows/:_id",
      "handler": "Show.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
